gang-chat.db
gang-chat.db-wal
gang-chat.db-shm
.env

# Build output (reproducible via `go build`)
/gang-server.exe
/gang-server
/dist/

# Deploy runtime (server-side, never committed)
/deploy/deploy.env
/deploy/logs/
/deploy/run/

# Local notes containing secrets — never commit
*.local.txt
