gang-chat.db
gang-chat.db-wal
gang-chat.db-shm
.env
assets/
assets-cache/

# Build output (reproducible via `go build`)
/.cache/
/gang-server.exe
/gang-server
/dist/

# Deploy runtime (server-side, never committed)
/deploy/deploy.env
/deploy/logs/
/deploy/run/

# Local notes containing secrets — never commit
*.local.txt

# Linux livekit binary — lives only on the deploy server, not in git
/livekit-server/livekit-server

# Codegraph index — tool-generated, machine-local
/.codegraph/
